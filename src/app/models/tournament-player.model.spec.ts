import { TournamentPlayer } from './tournament-player.model';

describe('TournamentPlayer', () => {
  it('should create an instance', () => {
    expect(new TournamentPlayer()).toBeTruthy();
  });
});
